#include <stdio.h>

int main(void) {

    int altura, largura;

    printf("Escreva a altura e a largura de um retângulo: ");
    scanf("%d %d", &altura, &largura);

    printf("A área do triângulo é: %d\n", (altura * largura));
}

#include <studio.h>

int main(void) {
    int a, b, c;

    printf("Insira os valores de um triângulo:\n");
    printf("A: ");
    scanf("%i", &a);

    printf("B: ");
    scanf("%i", &b);

    printf("C: ");
    scanf("%i", &c);

    if(a == b && b == c) {
        printf("O Triângulo é isósceles");
    }else if ()
}